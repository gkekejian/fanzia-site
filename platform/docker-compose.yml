services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: fanzia
      POSTGRES_PASSWORD: fanzia_dev_only
      POSTGRES_DB: fanzia_platform
    ports:
      - "55432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
